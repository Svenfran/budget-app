import { SpendingsOverviewUserDto } from './spendings-overview-user-dto';

describe('SpendingsOverviewUserDto', () => {
  it('should create an instance', () => {
    expect(new SpendingsOverviewUserDto()).toBeTruthy();
  });
});
