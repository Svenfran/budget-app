import { PasswordChangeDto } from './password-change-dto';

describe('PasswordChangeDto', () => {
  it('should create an instance', () => {
    expect(new PasswordChangeDto()).toBeTruthy();
  });
});
